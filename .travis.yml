language: node_js
sudo: false
node_js:
  - "node"
  - "6"
  - "5"
  - "4"

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  local_dir: docs
  repo: framevuerk/public
  target_branch: master
  fqdn: framevuerk.com
  script:
    - npm run build
  on:
    branch: master