fv-content
  div(:class="$root.mainClass")
    doc-description
      | To creating header inside 
      a.fv-link(@click="$router.push(`/components/fvMain`)") fvMain
      | , use this component. 
    doc-code
      = "<fv-header>\n"
      = "\t<!-- contents -->\n"
      = "</fv-header>\n"
    doc-example
      .fv-padding
        h4 Default:
        fv-header
          .title
            h2 Hello
        br
        h4 Custom elements:
        fv-header
          .title
            fv-input.fv-form-control.fv-search-style(placeholder="Type anything to search!")
          fv-button(icon="fa fa-search")
        br
        h4 With Buttons and description:
        fv-header
          fv-button(icon="fa fa-chevron-left")
          .title
            h2 Framevuerk
            p Support both rtl and ltr.
          fv-button(icon="fa fa-bars")
        br
        h4 With link:
        fv-header
          fv-button(icon="fa fa-chevron-left")
          .title
            h2 Twitter
          a.fv-link I'm Link
        br
        h4 Modified height:
        fv-header(:height="35")
          i.fa.fa-apple
          .title
            h4 Simple app
          small 18:49

    doc-api(:rows="api")
