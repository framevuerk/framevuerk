fv-main#app
  fv-header
    fv-button.fv-primary.fv-icon.fv-hide-on-lg(@click.native="$refs.sidebar.toggle()")
      i.fa.fa-bars
    .fv-title
      h2.fv-no-wrap(v-if="$route.name !== 'welcome'") {{$route.name}}
      h2.fv-no-wrap(v-else) Framevuerk
    .fv-icon
      a.fv-link(:href="$root.githubRepo + '/tree/master/src/components/' + $route.name",
        target="_blank") View Source
  router-view.fv-row
  fv-sidebar.sidebar(:pin="null", ref="sidebar", :width="270")
    transition(name="doc-sidebar-header")
      fv-header(:height="250", v-if="$route.name !== 'welcome'")
        a.fv-title.fv-text-center(@click="$router.push('/')")
          img.doc-logo(src="./logo-white.svg")
          h1 Framevuerk
          p.fv-word-wrap Awesome fully responsive Vue.js framevuerk!
    fv-tabs(:tabs="sidebarTabs")
      fv-list.fv-no-border(:items="sidebarItems", expanded, slot="components")
  fv-footer(:height="40")
    .fv-grow
    a.fv-link(:href="$root.githubRepo",
      target="_blank")
      i.fa.fa.fa-github
      |  Github page
