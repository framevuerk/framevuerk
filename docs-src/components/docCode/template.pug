span
  h3(v-if="title", v-html="title")
  h3(v-else) Code:
  .doc-code(v-if="$slots.default")
    pre(v-highlightjs)
      code(:class="lang")
        slot
  div(v-else)
    p.fv-text-center Nothing to show!
  br
