span
    fv-input(:display-value="displayValue", :value="value", @keydown.enter="open()", @click="open()")
    fv-dialog(ref="dialog", class="fv-select", :buttons="dialogButtons" @click="clickButton($event)" @keydown="pKeyDown($event)", @close="$emit('close')", @open="$emit('open')")
        fv-main(ref="select")
            fv-header(v-if="search || allowInsert")
                fv-input(class="fv-form-control fv-grow", placeholder="متن خود را وارد کنید...", v-model="searchQuery", ref="searchQueryEl")
            span(v-if="!search && !allowInsert", tabindex="0", ref="justFocusEl")
            fv-content.no-padding
                div(class="fv-option", v-show="pOptions.length == 0") چیزی پیدا نشد!
                div(class="fv-option", v-for="(option, index) in pOptions", v-html="option.text", @click="clickOption(option, true)", ref="optionElem",:class="{'fv-selected': pIsSelected(option), 'fv-focused': highlightedOption==index}")