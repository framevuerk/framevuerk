div.fv-radio
    span.fv-radio-check(:tabindex="option.disabled? '': 0", v-for="option in pOptions", @click="clickOption(option)", @keydown.enter="clickOption(option)", :class="{'fv-checked': isChecked(option), 'fv-disabled': option.disabled}")
        i.fv-radio-icon.fa(:class="{'fa-circle-o': !isChecked(option), 'fa-dot-circle-o': isChecked(option)}")
        ="  "
        label.fv-control-label(v-html="option.text")